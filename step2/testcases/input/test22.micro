PROGRAM sqrt
BEGIN

	STRING dummy := "abcde";  --This is dummy to satisfy the grammar

     INT i, j, iprime;
     
	FUNCTION VOID main()
	BEGIN
		i := 0;
		iprime := 0;
		j := 0;
		result := 0;
		
		WHILE (i < 100)
		     i := i + 1;
		     IF (i > 69)
		          CONTINUE;
		     ENDIF
		     iprime := iprime + 1;
		     WHILE (j < 100)
		          IF (j > 49)
		               BREAK;
		          ENDIF
		          j := j + 1;
	               result := result + 1;
		     ENDWHILE
		ENDWHILE

		WRITE (result);
		WRITE (j);
		WRITE (i);
	END
END
